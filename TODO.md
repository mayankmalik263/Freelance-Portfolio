# To Do List

- [ ] **Screenshots**: Update the `projects` array in `src/data/portfolio.ts` with the actual screenshot filenames, and add the image files to the `public/` directory. (e.g., `public/waffleda-screenshot.png`).
- [ ] **Pricing Data**: Open `src/data/portfolio.ts` and update the pricing numbers (currently set to placeholders like 15,000, 22,000, 35,000) to your final rates.
- [ ] **Case Study (Optional)**: Add the NewCycl Rawbin case study to `src/data/portfolio.ts` once you have permission.
- [ ] **Social Links (Optional)**: If you want to add social links (LinkedIn, Twitter), update the `Footer.tsx` and the `contactInfo` object in `src/data/portfolio.ts`.
